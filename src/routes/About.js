import React from 'react';
import './About.css';

const About = () => {
	return (
		<div className='about-container'>
			<span>A movie app that uses an API to fetch movies and displays them</span>
		</div>
	);
};

export default About;
